version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: finalcut-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: finalcut_social
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d

  redis:
    image: redis:7-alpine
    container_name: finalcut-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  api:
    build: ./backend
    container_name: finalcut-api
    ports:
      - "4000:4000"
    environment:
      - DB_HOST=postgres
      - DB_USER=admin
      - DB_PASSWORD=password
      - DB_NAME=finalcut_social
      - JWT_SECRET=super_secret_agent_key
    depends_on:
      - postgres
      - redis

  web:
    build: ./frontend
    container_name: finalcut-web
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=https://finalcut.ai/api
    depends_on:
      - api

volumes:
  postgres_data:
  redis_data: